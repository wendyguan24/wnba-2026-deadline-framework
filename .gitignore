# Raw and intermediate data are regenerable from R/01_download.R
data/raw/
data/processed/

# Synergy data is quarantined: team-aggregate numbers are typed in as cited
# constants in analysis/ prose, never committed as data files. See HANDOFF §3
# (Layer 2) and CLAUDE.md.
**/synergy*
**/*synergy*
analysis/*.csv
analysis/*.xlsx

# Raw third-party source documents (CBA text, FAQ exports) kept locally for
# reference extraction, not committed. The paraphrased, cited summary lives in
# data/reference/cba_rules_2026.md.
WNBA CBA Her Hoop Stats Faq.txt

# R artifacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# OS
Thumbs.db
Desktop.ini
